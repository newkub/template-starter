{
	"name": "packages",
	"type": "module",
	"scripts": {
		"dev": "bun run src/index.ts",
		"build": "tsdown",
		"lint": "biome check --staged --write && tsc --noEmit",
		"format": "biome format --write"
	},
	"dependencies": {
		"@clack/prompts": "latest"
	},
	"devDependencies": {
		"release-it": "latest"
	}
}
