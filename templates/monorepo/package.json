{
  "name": "nuxt-turborepo",
  "type": "module",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format",
    "update:deps": "bunx taze -r -w",
    "prepare": "bunx lefthook install"
  },
  "devDependencies": {
    "lefthook": "^2.0.12",
    "turbo": "^2.6.3"
  },
  "workspaces": [
    "apps",
    "config",
    "container",
    "packages/*"
  ]
}
