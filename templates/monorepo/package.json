{
  "name": "nuxt-turborepo",
  "packageManager": "bun@1.3.2",
  "type": "module",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format",
    "update:deps": "bunx taze -r -w",
    "prepare": "bunx lefthook install"
  },
  "devDependencies": {
    "lefthook": "^1.13.6",
    "turbo": "^2.6.1"
  },
  "workspaces": [
    "apps",
    "config",
    "container",
    "packages/*"
  ]
}
