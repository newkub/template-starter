$schema: 'https://moonrepo.dev/schemas/toolchain.json'

bun:
  version: '1.3.5'

javascript:
  version: 'latest'

typescript:
  version: 'latest'
  syncProjectReferences: true
  syncProjectConfig: true
