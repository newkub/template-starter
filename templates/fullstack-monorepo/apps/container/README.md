# Container

Docker/Podman container configuration for running the web app.

## Available Scripts

- `bun run build` - Build container image
- `bun run run` - Run container
- `bun run apps:build` - Build web app
- `bun run apps:dev` - Run web app in development
- `bun run apps:start` - Start web app
