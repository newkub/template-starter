{
	"name": "@templates/template-cli",
	"version": "1.0.0",
	"description": "CLI to scaffold, view, and manage project templates",
	"keywords": [
		"cli",
		"templates",
		"scaffold"
	],
	"author": "Wrikka",
	"license": "MIT",
	"type": "module",
	"private": false,
	"bin": {
		"templates": "./dist/main.js"
	},
	"files": [
		"dist",
		"README.md"
	],
	"scripts": {
		"dev": "bunx bunup --watch",
		"build": "bunx bunup",
		"start": "bun run src/main.ts",
		"format": "biome format --write .",
		"lint": "biome lint .",
		"typecheck": "tsc --noEmit",
		"test": "bun test",
		"verify": "bun run typecheck && bun run lint && bun run test",
		"ci": "bun run verify && bun run build"
	},
	"dependencies": {
		"cac": "^7.0.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.16",
		"@types/bun": "latest",
		"@types/node": "^25.9.2",
		"bunup": "^0.16.32",
		"typescript": "^6.0.3"
	}
}
