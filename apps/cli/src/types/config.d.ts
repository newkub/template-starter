export interface TemplateConfig {
  ecosystem: string
  libraries: string[]
}

export interface Library {
  id: string
  name: string
  description: string
  icon: string
  category: string
}