{
  "name": "templates",
  "version": "0.0.0",
  "private": true,
  "packageManager": "bun@1.3.2",
  "type": "module",
  "workspaces": [
    "template/*",
    "cli"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean && bunx rimraf node_modules",
    "ci": "turbo run build lint test",
    "update:deps": "bunx taze -r -w"
  },
  "devDependencies": {
    "turbo": "^2.6.1"
  }
}
